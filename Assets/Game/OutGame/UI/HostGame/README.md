# Host Game

닉네임과 임시 6자리 방 코드를 준비한 뒤 같은 `MainMenu` 씬의 Lobby 패널로 이동하는 로컬 UI 프로토타입입니다. 실제 방 생성이나 네트워크 연결은 수행하지 않습니다.

- `CREATE ROOM` → `view-lobby`
- `BACK` → `view-home`
- 독립 씬은 없으며 레이아웃은 `MainMenu/MainMenu.uxml`에 포함됩니다.
