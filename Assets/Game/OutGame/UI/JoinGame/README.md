# Join Game

닉네임과 방 코드를 검증한 뒤 같은 `MainMenu` 씬의 Lobby 패널로 이동하는 로컬 UI 프로토타입입니다. 코드를 비워 두면 같은 화면에서 안내하고 이동하지 않습니다.

- `JOIN ROOM` → `view-lobby`
- `BACK` → `view-home`
- 실제 서버 조회나 네트워크 접속은 수행하지 않습니다.
- 독립 씬은 없으며 레이아웃은 `MainMenu/MainMenu.uxml`에 포함됩니다.
