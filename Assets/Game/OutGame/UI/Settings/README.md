# Settings

OutGame UI에 필요한 최소 설정을 제공하는 `MainMenu` 내부 패널입니다.

- UI 사운드 설정은 `PlayerPrefs`에 저장되어 다음 OutGame 씬에서도 유지됩니다.
- Quality와 Fullscreen 버튼은 런타임 Unity 설정을 즉시 변경합니다.
- `BACK`은 항상 같은 씬의 `view-home`으로 돌아갑니다.
- 독립 씬은 없으며 레이아웃은 `MainMenu/MainMenu.uxml`에 포함됩니다.
