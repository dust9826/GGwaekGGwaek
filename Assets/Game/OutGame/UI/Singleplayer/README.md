# Singleplayer

레거시 싱글플레이 준비 레이아웃입니다. `DEPLOY SNOWPLOW`는 `LoadingScreen` 씬을 거쳐 `SinglePlay`로 진입합니다. 현재 Home의 `CHOOSE ROUTE`는 같은 씬의 Stage Select 패널을 먼저 열고, 그곳의 `DEPLOY SNOW CREW`도 같은 로딩·게임 진입 경로를 사용합니다.

- `BACK` → `view-home`
- 독립 씬은 없으며 레이아웃은 `MainMenu/MainMenu.uxml`에 포함됩니다.
